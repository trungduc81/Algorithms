
Đề DSA06024 : Hãy thực hiện thuật toán sắp xếp chọn trên dãy N số nguyên. Ghi ra các bước thực hiện thuật toán.

Dữ liệu vào: Dòng 1 ghi số N (không quá 100). Dòng 2 ghi N số nguyên dương (không quá 100).

Kết quả: Ghi ra màn hình từng bước thực hiện thuật toán. Mỗi bước trên một dòng, các số trong dãy cách nhau đúng một khoảng trống.
Input 
4
5 7 3 2
Ouput : 
Buoc 1: 2 7 3 5

Buoc 2: 2 3 7 5

Buoc 3: 2 3 5 7

#include <bits/stdc++.h>
using namespace std;

// Ham sap xep chon (Selection Sort)
void selection_sort(int a[], int n) {   
    int cnt = 0; // Dem so buoc sap xep

    for (int i = 0; i < n - 1; i++) {
        int min = i; // Gia su vi tri nho nhat la i

        // Tim phan tu nho nhat trong doan tu i+1 den n-1
        for (int j = i + 1; j < n; j++) {
            if (a[j] < a[min]) {
                min = j;  
            }
        }

        // Doi cho a[i] voi phan tu nho nhat neu can thiet
        if (min != i) {
            swap(a[i], a[min]); 
        } 

        cnt++; // Tang bien dem buoc sap xep
        cout << "Buoc " << cnt << ": "; 

        // In mang sau moi buoc sap xep
        for (int i = 0; i < n; i++) {
            cout << a[i] << " ";  
        }
        cout << endl; 
    }
}

int main() {
    int n;  
    cin >> n; // Nhap so phan tu cua mang
    int a[n];  

    // Nhap mang
    for (int i = 0; i < n; i++) {
        cin >> a[i];  
    }

    // Goi ham sap xep chon
    selection_sort(a, n);  
}
